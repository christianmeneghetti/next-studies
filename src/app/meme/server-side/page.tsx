import { CardsSSR } from '@/components/Cards/CardsSSR'

export default function SSRListing() {
  return (
    <div className="flex fixed inset-0 flex-col items-center justify-center p-2">
      <CardsSSR />
    </div>
  )
}
